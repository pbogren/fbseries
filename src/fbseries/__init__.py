"""Init fbseries package.."""

# Virtualenv adds the module root directory to path. If the module is run
# outside of virtual environment it has to added manually for the tests to work
# In that case uncomment these lines.
# import sys, os
# sys.path.append(os.path.abspath("./src"))
